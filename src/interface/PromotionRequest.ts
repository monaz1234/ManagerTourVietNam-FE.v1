interface PromotionRequest {
    idtour: string | null;         // Mã tour, giả sử là số
    code: string; // Mã khuyến mãi, kiểu chuỗi
    quantity: number;       // Số lượng tour, kiểu số
  }
  

  